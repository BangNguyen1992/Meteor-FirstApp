import { Meteor } from 'meteor/meteor';
import '../custom/main.js';

Meteor.startup(() => {
  // code to run on server at startup
  // MyList.insert({
  //   _id: 'liana',
  //   clickCount: Number
  // });

});

